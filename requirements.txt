pandas
scikit-learn
pytest
fastapi
uvicorn[standard]
pydantic
httpx
opentelemetry-api
opentelemetry-sdk
# fairlearn  # Will add later when network is stable
# shap       # Will add later when network is stable  
# matplotlib # Will add later when network is stable